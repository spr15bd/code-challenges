function permAlone(str) {
  let perms = 0
  let charArray = [] 
  for (let i=0; i<str.length; i++) {
    charArray[i] = str.charAt(i)
    perms++
  }
  console.log(charArray)
  return perms
}
permAlone('aab')

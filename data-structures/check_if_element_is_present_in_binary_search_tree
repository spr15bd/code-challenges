var displayTree = tree => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}
function BinarySearchTree() {
  this.root = null;
  // Only change code below this line
  this.isPresent = function(num) {
    if (!this.root) {
      return false
    }
    let currentItem = this.root
    while (currentItem) {
      if (currentItem.value === num) {
        return true
      }
      if (!currentItem.value) {
        return null
      }
      let bst = new BinarySearchTree() 
      bst.root = currentItem.left
      return bst.isPresent(num)
    }
  }
  // Only change code above this line
}

// test
let bs = new BinarySearchTree()
bs.root = new Node(14)
bs.root.left = new Node(8)
bs.root.right = new Node(28)

var displayTree = tree => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}
function BinarySearchTree() {
  this.root = null;
  // Only change code below this line
  this.findMin = function() {
    let min
    if (!this.root) {
      return null
    } else {
      let currentMin
      let currentNode
      currentNode = this.root
      currentMin = this.root.value
      while (currentNode) {
        currentNode = currentNode.left
        currentMin = currentNode.left
      }
      return currentMin
    }
  }
  this.findMax = function() {
    let max
    if (!this.root) {
      return null
    } else {
      let currentMax
      let currentNode
      currentNode = this.root
      currentMax = this.root.value
      while (currentNode) {
        currentNode = currentNode.right
        currentMax = currentNode.right
      }
      return currentMax
    }
  }
  // Only change code above this line
}

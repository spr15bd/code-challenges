var Node = function(data, prev) {
  this.data = data;
  this.prev = prev;
  this.next = null;
};
var DoublyLinkedList = function() {
  this.head = null;
  this.tail = null;
  // Only change code below this line
  this.add = function(element) {
    if (!this.head) {
      this.head = new Node(element, null)
      this.tail = this.head
    } else {
      let newTail = new Node(element, this.tail)
      this.tail.next = newTail
      this.tail = newTail
    }
  }
  this.reverse = function() {
    if (!this.head) {
      return null
    } else {
      let currentNode = this.head
      while (currentNode) {
        current
        currentNode.next = currentNode.prev
        currentNode.prev = null
        console.log("head prev: ", this.head.prev)
        console.log("head next: ", this.head.next)
      }
      
      while (currentNode.next) {
        if (currentNode.next === this.tail) {
          this.tail.prev=currentNode.next.next
          this.tail.next = null
          currentNode = this.tail
          console.log("tail prev: ", this.tail.prev)
      console.log("tail next: ", this.tail.next)
        } else {
          currentNode = currentNode.next
          let temp = currentNode
          currentNode.next = currentNode.prev
          currentNode.prev = temp.next
        }
      }
    }
  }
  // Only change code above this line
};

let dll = new DoublyLinkedList()
dll.add("123")
dll.add("222")
dll.add("234")
dll.add("333")

console.log("dll is ", dll)
dll.reverse(dll)
console.log("dll reversed once is ", dll)
dll.reverse(dll)
console.log("dll reversed twice is ", dll)

var Node = function(data, prev) {
  this.data = data;
  this.prev = prev;
  this.next = null;
};
var DoublyLinkedList = function() {
  this.head = null;
  this.tail = null;
  // Only change code below this line
  this.reverse = function(list) {
    if (!list) {
      return null
    } else {
      let temp = this.tail
      this.tail = this.head
      this.head = temp
      //this.tail.prev = this.head
      //this.tail.next = null
      
      //this.head.next = this.tail
      //this.head.prev = null

      let currentNode = this.head
      currentNode.next = currentNode.prev
      currentNode.prev = null
      
      while (currentNode.next && currentNode.next !== this.tail) {
        currentNode = currentNode.next
        let temp = currentNode
        currentNode.next = currentNode.prev
        currentNode.prev = temp.next
      }
    }
  }
  // Only change code above this line
};

let dll = new DoublyLinkedList()
dll.head = new Node("123", null)
let middleNode = new Node("222", dll.head)
dll.head.next = middleNode
dll.tail = new Node("234", middleNode)
middleNode.next = dll.tail

console.log("dll is ", dll)
dll.reverse(dll)
console.log("dll reversed is ", dll)

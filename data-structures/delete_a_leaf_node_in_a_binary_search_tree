var displayTree = tree => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}

function BinarySearchTree() {
  this.root = null;
  // Only change code below this line
  this.remove = function (targetNode) {
    console.log("removing: ", targetNode)
    if (!this.root) return null
    let foundTargetNode = false
    // find node
    while (!foundTargetNode) {
      if (this.root.value === targetNode) { // found the node we're looking for
        console.log("found ", targetNode)
        foundTargetNode = true
      
      } else if (targetNode<this.root.value) { // didn't find it
        console.log("didnt find node")
        if (!this.root.left) return null
        this.root = this.root.left
      } else {
        if (!this.root.right) return null
        this.root = this.root.right
      }
    }
    if (!this.root.left && !this.root.right) {
      // set parent to null
    }
  }
}



// test
let bst = new BinarySearchTree()
let node = new Node(18)
node.left = new Node(9)
node.left.left = new Node(4)
node.left.right = new Node(13)
node.left.right.left = new Node(11)
node.left.right.left.left = new Node(10)
node.right = new Node(27)
node.right.left = new Node(23)
node.right.right = new Node(36)
node.right.right.left = new Node(35)
node.right.right.right = new Node(40)
bst.root = node
bst.remove(11)

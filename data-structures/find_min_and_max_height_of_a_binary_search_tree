var displayTree = tree => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}
function BinarySearchTree() {
  this.root = null;
  // Only change code below this line
  this.findMinHeight = function() {
    if (!tree) {
      return -1
    }
    this.findNodeMinHeight = function(node) {
      if (node) {
        return findNodeMinHeight(node.left) 
        && findNodeMinHeight(node.right)
      }
    }
  }
  this.findMaxHeight = function() {
    if (!tree) {
      return -1
    }
  }
  this.isBalanced = function() {
    return false
  }
  // Only change code above this line
}

// test
let bst = new BinarySearchTree()
let node = new Node(18)
node.left = new Node(9)
node.left.left = new Node(4)
node.left.right = new Node(13)
node.right = new Node(27)
node.right.left = new Node(33)
node.right.left = new Node(23)
node.right.right = new Node(36)
bst.root = node
console.log(bst.findMinHeight())

var displayTree = tree => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}
function BinarySearchTree() {
  this.root = null;
  // Only change code below this line
  this.findMinHeight = function() {
    let height = 0
    if (!this.root) {
      return -1
    } else {
      return findNodeMinHeight(this.root)
    }
    function findNodeMinHeight (node) {
      if (node) {
        height++
        return Math.min(findNodeMinHeight(node.left)
        && findNodeMinHeight(node.right))
      } else {
        return height
      }
    }
  }
  this.findMaxHeight = function(tree) {
    let height = 0
    if (!this.root) {
      return -1
    } else {
      return findNodeMaxHeight(this.root)
    }
    function findNodeMaxHeight (node) {
      if (node) {
        height++
        return Math.max(findNodeMaxHeight(node.left)
        && findNodeMaxHeight(node.right))
      } else {
        return height
      }
    }
  }
  this.isBalanced = function() {
    return findMinHeight === findMaxHeight
  }
  // Only change code above this line
}

// test
let bst = new BinarySearchTree()
let node = new Node(18)
node.left = new Node(9)
node.left.left = new Node(4)
node.left.right = new Node(13)
node.right = new Node(27)
node.right.left = new Node(33)
node.right.left = new Node(23)
node.right.right = new Node(36)
bst.root = node
console.log(bst.findMinHeight())

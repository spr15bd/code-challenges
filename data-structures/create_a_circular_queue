class CircularQueue {
  constructor(size) {

    this.queue = [];
    this.read = 0;
    this.write = 0;
    this.max = size - 1;

    while (size > 0) {
      this.queue.push(null);
      size--;
    }
  }

  print() {
    return this.queue;
  }

  enqueue(item) {
    // Only change code below this line
    if (this.write <= this.read) {
      console.log("length before enqueue ", this.queue.length)
      this.queue[this.write] = item
      if (this.queue[this.write] === item) {
        return item
      } else {
        return null
      }
      console.log("length after enqueue ", this.queue.length)
      this.write++
      if (this.write > this.queue.length) {
        this.write = 0
      }
    }
    // Only change code above this line
  }

  dequeue() {
    // Only change code below this line
    let itemToReturn = this.queue.shift()
    if (itemToReturn) {
      console.log("dequeued ", itemToReturn)
      return itemToReturn
    } else {
      return null
    }
    // Only change code above this line
  }
}

let c = new CircularQueue(3)
console.log("c before is ", c)
c.enqueue('h')
c.enqueue('v')
c.enqueue('w')
console.log("c is ", c)

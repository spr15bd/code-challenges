function LinkedList() {
  var length = 0;
  var head = null;

  var Node = function(element){
    this.element = element;
    this.next = null;
  };

  this.head = function(){
    return head;
  };

  this.size = function(){
    return length;
  };

  this.add = function(element){
    // Only change code below this line
    if (this.length === 0) {
      let newItem = new Node(element)
      this.head = newItem
    this.head = newItem
    } else {
      
    }
    this.length++
    // Only change code above this line
  };
}

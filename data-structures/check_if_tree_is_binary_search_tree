var displayTree = (tree) => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}
function BinarySearchTree() {
  this.root = null;
}
function isBinarySearchTree(tree) {
  // Only change code below this line
  //check if root is a bst
  
  function isBinarySubTree(tree) {
    return !tree || ((!tree.left || tree.left.value < tree.value) && (!tree.right || tree.value <= tree.right.value) && isBinarySubTree(tree.left) && isBinarySubTree(tree.right))
  }
    
  return isBinarySubTree(tree.root)

  // Only change code above this line
}

// test
let bst = new BinarySearchTree()
let node = {}
bst = node
console.log(isBinarySearchTree(bst))

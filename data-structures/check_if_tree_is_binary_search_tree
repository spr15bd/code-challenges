var displayTree = (tree) => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}
function BinarySearchTree() {
  this.root = null;
}
function isBinarySearchTree(tree) {
  // Only change code below this line
  //check if root is a bst
  let subTree = tree.root
  console.log("subtree is ", subTree)
  console.log("subtree left is ", subTree.left.value)
  console.log("subtree value is ", subTree.value)
  console.log("subtree right is ", subTree.right.value)
  if (subTree.left.value <= subTree.value && subTree.value < subTree.right.value) {
    console.log("subtree is a bst")
    return isBinarySearchTree(subTree.root)
  } else {
    return false
  }

  if (isBinarySearchTree(bst.root)) {
    return true
  } else {
    return false
  }
  // Only change code above this line
}

// test
let bst = new BinarySearchTree()
let node = new Node(18)
node.left = new Node(9)
node.left.left = new Node(4)
node.left.right = new Node(13)
node.right = new Node(27)
node.right.left = new Node(23)
node.right.right = new Node(36)
bst.root = node

console.log("bst is ", bst)
console.log("Is this a bst? ", isBinarySearchTree(bst))

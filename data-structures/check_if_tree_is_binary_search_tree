var displayTree = (tree) => console.log(JSON.stringify(tree, null, 2));
function Node(value) {
  this.value = value;
  this.left = null;
  this.right = null;
}
function BinarySearchTree() {
  this.root = null;
}
function isBinarySearchTree(tree) {
  // Only change code below this line
  //check if root is a bst
  let subTree = tree
  if (!subTree.value) return true
  if (subTree.left?.value && subTree.right?.value && subTree.left?.value <= subTree.value && subTree.value < subTree.right?.value) {
    return isBinarySearchTree(subTree.left)
    && isBinarySearchTree(subTree.right)
  } else if (subTree.right?.value && !subTree.left?.value && subTree.value < subTree.right?.value){
    return isBinarySearchTree(subTree.right)
  } else if (subTree.left?.value && !subTree.right?.value && subTree.left?.value <= subTree.value) {
    return isBinarySearchTree(subTree.left)
  } else {
    console.log("reached base, returning true")
    return true
  }

  // Only change code above this line
}

// test
let bst = new BinarySearchTree()
let node = new Node(18)
node.left = new Node(9)
node.left.left = new Node(4)
node.left.right = new Node(13)
node.right = new Node(27)
node.right.left = new Node(33)
node.right.right = new Node(36)
bst = node
console.log(isBinarySearchTree(bst))

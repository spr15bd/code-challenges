function pairwise(arr, arg) {
  arr.forEach((num,idx)=>{
    for (let x=0; x<arr.length; x++) {
      if (num + arr[x] === arg) {
        arg = idx + x
      }
    }
  })
  return arg;
}

pairwise([1,4,2,3,0,5], 7);

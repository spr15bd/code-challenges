function pairwise(arr, arg) {
  let finalSumOfIndices = 0
  arr.forEach((num,idx)=>{
    for (let x=idx+1; x<arr.length; x++) {
      if (num + arr[x] === arg) {
        finalSumOfIndices += idx + x
      }
    }
  })
  return finalSumOfIndices;
}

console.log(pairwise([1,4,2,3,0,5], 7))
